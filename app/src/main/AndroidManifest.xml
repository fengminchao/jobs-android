<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muxistudio.jobs">

  <uses-permission android:name="android.permission.CAMERA"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.READ_PHONE_STATE" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.READ_LOGS" />

  <application
      android:name=".App"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppThemeLight">

    <activity
        android:name=".ui.main.MainActivity"
        android:theme="@style/AppThemeLight">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity android:name=".ui.accout.AccountActivity"/>

    <activity android:name=".ui.accout.AccountEditActivity"/>

    <activity android:name=".ui.login.LoginActivity"/>

    <activity android:name=".ui.register_forget.SetPwdActivity"/>

    <activity android:name=".ui.find.detail.CareerDetailActivity"/>

    <activity android:name=".ui.find.detail.FulltimeDetailAcitivity"/>

    <activity android:name=".ui.find.detail.EmployDetailActivity"/>

    <activity android:name=".ui.forum.EditPostActivity"/>

    <activity android:name=".ui.forum.PostDetailActivity"/>

  </application>


</manifest>
